# 構造整合FEM設計 — OGWオメガストリンガ板に合わせたガイド波シミュレーション

**目的**: 現状のsim-to-real失敗の真因は「学習FEM=湾曲ハニカムフェアリング／検証データ=OGW平板+オメガストリンガ」の**構造ミスマッチ**。本設計は **OGWと同一構造のFEM** を生成し、公平な FEM→OGW 転移（学習FEM／検証OGW）を成立させる。Abaqus実行はExplicit並列ライセンス復活待ちのため、ここでは**仕様のみ**確定する。

関連: [[ROADMAP.md]] Phase1/4、`scripts/parse_ogw_wavefield.py`（OGW実測の参照）、記憶 project_payload_link。

---

## 0. OGW実測スペック（合わせるべき基準）
`data/external/ogw_stringer/` の Readme + HDF5 実データより確定:
- 構造: **CFRP平板 + オメガ（hat断面）ストリンガ**を片面に接着。
- 走査領域: **約 500 × 500 mm**（座標 0.009–0.491 m）、SLDV 約 **1 mm 格子・233,289点**、面外応答 `u_z`。
- 時間: **1024 ステップ・dt ≈ 0.78 µs（fs ≈ 1.28 MHz）・全長 ≈ 0.8 ms**。
- 加振: ストリンガ側面中央に PZT。**5サイクル Hann トーンバースト @ 16.5/50/100/200/300 kHz** ＋ **チャープ 20–500 kHz**。
- 欠陥: **背面衝撃由来のスキン–ストリンガ剥離**、3状態 = 健全 / 局所剥離 / 大剥離。

→ FEMはこれらを**同じ単位・同じ出力形式**で再現する（変換器 `ogw_to_sensor_csv.py` がそのまま使える）。

---

## 1. ジオメトリ
- 平板: 500 × 500 mm、板厚 t_skin（OGW実測の分散関係から逆同定。初期値 **2 mm**・準等方）。
- オメガストリンガ: 板中央を縦断（または実測RMS帯位置に合わせる）。hat断面（フランジ幅・ウェブ高・天面幅は標準値で開始、実測の群速度/反射で校正）。
- 接着層: スキン–ストリンガ・フランジ間に**コヒーシブ層**（厚さ ~0.1–0.2 mm）。

## 2. 材料・積層
- CFRP: 準等方積層（例 [45/0/-45/90]s）。弾性定数は **OGW健全波動場の分散関係(A0/S0)にフィット**して同定（最重要キャリブレーション）。
- 減衰: Rayleigh または材料減衰を実測の振幅減衰に合わせる（sim-to-real振幅整合に効く）。
- 接着/コヒーシブ: traction-separation（K, t⁰, G_c）。剥離=コヒーシブ損傷 or 事前非接着領域。

## 3. 欠陥モデル（3状態をOGWに対応）
- **健全**: 完全接着。
- **局所剥離 / 大剥離**: スキン–ストリンガ界面の**非接着領域**（contact, no-tie）または**コヒーシブ要素の事前損傷**で、OGWの局所/大に面積を合わせる（局所 ~Ø20–30 mm 相当、大 ~その3–5倍。実測差分RMSの広がりで調整）。
- 位置: ストリンガ脚部。DOEで複数位置に振る。

## 4. 物理・ソルバ
- **Abaqus/Explicit 動的**（ガイドLamb波）。
- 安定増分 Δt_stable ≈ L_e / c_d。**質量スケーリング禁止**（波動問題＝伝播速度を変える）。
- 出力時間刻みは **1.28 MHz にデシメーション**して OGW と一致、全長 0.8 ms（≈1024点）。

## 5. メッシュ（最重要・コスト支配）
- 面内要素寸法: **λ_min / 15 以上の解像度**。λ_min は最高周波数(300 kHz)の **A0モード波長**（CFRPで数 mm オーダー）から決定 → 面内 **~0.3–0.5 mm**。
- 板厚方向: continuum shell or 複数ソリッド層（A0/S0両モード再現に厚み方向2–4層）。
- 要素型: 縮約積分ソリッド(C3D8R) or continuum shell(SC8R)。界面にコヒーシブ(COH3D8)。
- 概算規模: 500×500 mm を 0.4 mm 面内 → **~1.5×10⁶ 要素/層**。⇒ 後述のコスト対策必須。

## 6. 加振
- PZTを集中力 or 圧電要素で近似。OGWと同位置（ストリンガ側面中央）。
- 入力: **5サイクル Hann トーンバースト @ 50/100/200/300 kHz**（＋必要ならチャープ）。各周波数で別ラン。

## 7. 出力 → グラフ → 学習
- 上面ノードの **面外速度 u_z** を ~1 mm 格子で抽出 → `coordinates/data_z/time` 相当（OGWと同形式）。
- 既存 `ogw_to_sensor_csv.py`（n=9/99 サブサンプル）→ `build_gw_graph(feature_set=comprehensive)` → `train_gw` がそのまま流用可。
- **学習=この整合FEM、検証=OGW実測**で公平な sim-to-real。

## 8. DOE（データセット計画）
| 因子 | 水準 |
|---|---|
| 欠陥 | 健全 / 局所 / 大（+位置 3–5） |
| 周波数 | 50/100/200/300 kHz |
| 健全反復 | メッシュ/材料微小摂動で N_healthy 増（**校正用の複数健全**＝今回の欠落を解消） |
- 目標 ~数十〜百ラン。**健全を複数**にして「実測健全≠FEM健全」校正問題を緩和。

## 9. 計算コスト & ライセンス対策
- 1.5M要素×Explicit×0.8ms は重い。並列(DMP)ライセンス制約下の対策:
  1. **領域縮小**: 加振近傍 ~300×300 mm + 吸収境界(無限要素/減衰帯)でエッジ反射抑制。
  2. **対称性**: ストリンガ軸で半モデル（健全/対称欠陥のみ）。
  3. **cpus=1 Explicit**は通る（遅い）→ 1ランの実時間を試算してから本生成。
  4. 周波数別に最高周波数のみ高解像度、低周波は粗メッシュ流用。
- まず **1ケース（健全・100 kHz）を試走**し、OGW健全と**分散関係/群速度を照合**（材料同定＝Phase0）。合えば横展開。

## 10. 検証（sim-to-real成立条件）
- **Step A（物理校正）**: FEM健全 vs OGW健全の分散曲線・到達時間一致（材料定数同定）。
- **Step B（転移）**: 整合FEMで学習 → OGW 3状態を分類。構造が揃ったので、今回の「全部欠陥に飽和」が解消するはず。
- **Step C（残差対策）**: なお残るギャップは baseline差分特徴（WCCM差分正規化と同原理）＋ Rayleigh減衰整合で詰める。

---

## 実装順（Abaqus復帰後）
1. 材料同定ラン（健全・100 kHz・1ケース）→ OGW分散にフィット。
2. メッシュ収束（λ/15 確認）。
3. DOE本生成（健全複数＋局所/大×位置×周波数）。
4. ODB→u_z格子→既存GWパイプライン→学習→OGW検証。
