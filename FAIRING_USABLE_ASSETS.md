# 衛星(ペイロード)フェアリング — 使える資産インベントリ（2026-06-20 整理）

> 「14/100」は **静的Max-Q応力データセット**のスコア。フェアリングには別に
> **GW(guided-wave)動解析の検出パイプライン**があり、そちらは成熟・使用可能。
> 混同しないため2トラックに分けて整理する。

## トラックA：guided-wave 動解析 検出（成熟・使える）🟢

波動伝播の explicit 動解析。SHM検出（健全 vs 欠陥分類）の本命。SAGE GNN で **F1 0.788** 達成済みの土台。

| 資産 | 数 | サイズ/本 | 状態 | 用途 |
|---|---|---|---|---|
| `abaqus_work/Job-GW-Fair-0000〜0099` | ~100 | 2.24 GB | ✅全COMPLETED | 主データセット（パラメトリック欠陥） |
| `abaqus_work/Job-GW-Fair-*_sensors.csv` | 100 | — | ✅抽出済 | **GNN学習入力ready** |
| `Job-GW-Curved-H / -D` | 2 | 9.8 GB | ✅ | 曲面フェアリング 健全/欠陥 |
| `Job-GW-D2-r15 / D3-r40 / D4-near / D5-edge` | 4 | 8.5 GB | ✅ | 欠陥位置・半径バリアント |
| `Job-GW-Debond / -v2 / Healthy` 他 | — | 8.5 GB | ✅ | 健全/debond基準 |
| **GW odb 合計** | **133** | — | **全✅** | 〜300 GB級。消さないこと |

→ **次の一手**：sensor CSV 100本で GNN を回す（既に F1 0.788 実績、再現/拡張が即可能）。

## トラックB：静的 Max-Q 応力 / SCF データセット（再構築中）🔴→🟡

各欠陥タイプの Max-Q 空力荷重下の応力場（Mises/DSPSS）→ SCF表・GNNノード特徴量。

| 項目 | 状態 |
|---|---|
| 旧 `H3_*_mq.odb` / `_t2` / 試作65本 | ❌**全削除済**（cohesive 0.2mm で応力0・11.3GB回収） |
| 真因 | **cohesive接着層 0.2mm→歪み要素で求解不能**（既知。datacheck通過し実solveで死亡） |
| 正解 | `--adhesive_thickness 2.0` で再生成 |
| `H3_healthy_a2`（2.0mm版） | 🔄 **検証solve中(40506)**。tie drops 0（前回88）＝好転 |
| 唯一残った有効静解析 | `H3_Acoustic_0000.odb`（106 MPa） |

→ **次の一手**：a2が `COMPLETED` 確認 → 残り7シナリオ（debond/fod/impact/delam/acoustic/inner/thermal）を 2.0mm で一括再生成→solve → SCF表有効化。

## 注意（消さない / 要確認）
- **トラックA(GW)は全部有効＝絶対消さない**（〜300GB だが資産価値）。
- 非ゼロだが異常高（BC特異疑い・要確認）：`H3_Healthy_old_noSymBC`(9247), `H3_Test_C3D10_f02`(9995), `H3_Test_v3_f02`(2221)。0応力ではないので保持中、不要なら別途削除。

## 一行サマリ
- **今すぐ論文/GNNに使えるフェアリング資産＝トラックA（GW動解析133本＋CSV100本、F1 0.788）**。
- トラックB（静的応力）は再構築中で、a2収束が緑になれば復活する。
